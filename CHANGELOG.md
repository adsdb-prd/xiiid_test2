# 코드 정리 및 검증 내역

## 변경 결과

| 항목 | 원본 | 정리본 |
|---|---:|---:|
| 브라우저용 CSS | 5개 | `site/brand/site.css` 1개 |
| 브라우저용 JS | 3개 | `site/brand/site.js` 1개 |
| 메인 페이지의 CSS·JS 요청 | 7개 | 2개 |
| Token 페이지의 CSS·JS 요청 | 7개 | 2개 |

- 기존 스타일 순서와 JS의 독립 함수 범위를 유지하며 통합했습니다.
- 동일 조건의 중복 CSS 규칙 13개와 현재 HTML·JS에서 사용하지 않는 CSS 규칙 38개를 삭제했습니다. 상세 선택자는 `cleanup-manifest.json`에 있습니다.
- 호출되지 않는 WebGL 행렬 함수 `multiply()`를 삭제했습니다. 실제 셰이더·메시·카메라 계산은 유지했습니다.
- 미사용 이미지 `site/brand/partners/xidsoft.png`와 폰트 `site/webfonts/ABCRepro-Regular.woff2`를 삭제했습니다. 삭제한 자산 합계는 271,469바이트(약 265.1 KiB)입니다.
- 빌드 스크립트에서 더 이상 존재하지 않는 Nuxt·Sanity·local.js 관련 경로 처리를 제거했습니다. 루트와 하위 경로 배포는 유지했습니다.
- HTML 들여쓰기, CSS 속성별 줄바꿈, JS 함수·배열·셰이더 문자열 줄바꿈을 정리했습니다. 기능별 안내 주석과 개발자용 README를 작성했습니다.
- 사용 중인 이미지·SVG·영상·폰트는 모두 원본 바이트를 유지했습니다. 본문, 링크, 검색 차단 설정, 소셜 공유 정보, 시세 API, Blog 갱신 및 배포 워크플로도 유지했습니다.

중복처럼 보여도 끊김 없는 로고 이동에 필요한 HTML 복제본, 반응형 덮어쓰기, 브라우저 대체 속성, WebGL 및 동작 줄이기 상태의 대체 스타일은 보존했습니다. 파일을 합쳤으므로 Token 페이지도 공통 JS를 받지만 3D 대상이 없으면 해당 영역을 초기화하지 않습니다. 최소 파일 수와 유지보수를 우선한 구성입니다.

## 화면 비교

Chrome에서 메인·Token 두 페이지를 1440, 1024, 768, 767, 390, 360px 너비로 비교했습니다. 긴 모바일 화면은 구간별로 나누어 끝까지 캡처했습니다.

초기 통합본 검증 12개 조합에서 요소 위치·주요 계산 스타일 및 비교 이미지의 픽셀 차이는 모두 0이었습니다. 움직이는 WebGL·영상은 화면 비교에서 제외하고, 애니메이션과 시세 응답은 고정해 비교했습니다. 마지막 줄바꿈 후에도 1440·390px 두 페이지의 픽셀 비교가 일치했습니다.

## 기능·구조 검증

- 모바일 메뉴 열기, 메뉴 선택 후 닫기, Projects 앵커 이동
- AI Tutor / AI Studio / Blockchain 모달 열기와 Escape 닫기
- News 다음 버튼 이동, Blog 3개 표시
- 시세 티커 1회 복제, 성공 응답 표시, Coinbase 실패 시 CoinGecko 대체 조회, 전체 API 실패 시 Unavailable 표시
- Blog 조회 실패 시 Medium 안내 표시
- 활동 카운터 완료, WebGL 캔버스 1회 생성 및 GL 오류 없음, WebGL 미지원 대체 배경
- Token 왕복 이동, Token의 3D 초기화 생략, 분배 SVG 애니메이션
- JavaScript를 껐을 때 정적 본문 표시
- 모든 로컬 자산 경로, 통합 JS 및 Node 스크립트 문법 검사
- 실제 동봉 빌드·서버 스크립트로 `/`와 `/preview/` 경로 모두 확인
- 원본 JS와 통합 JS의 실행 구조(AST) 비교: 미사용 함수 삭제와 명시한 초기화 조건을 제외하면 동일. 마지막 줄바꿈도 검증된 통합본과 AST가 동일

수정본의 최종 통합 테스트에서 브라우저 JS 실행 오류는 없었습니다. 자세한 테스트 기록은 `validation-report.json`에 있습니다.

## 검증 범위와 제한

외부 시세 API는 재현 가능한 테스트 응답으로 성공·실패를 검증했습니다. 실시간 서비스 가용성, 실제 GitHub Pages 배포, Safari·Firefox 및 실제 모바일 기기 테스트는 수행하지 않았습니다. 자동 화면 비교에서 고정한 WebGL·카운터·도넛 애니메이션은 별도로 정상 동작을 확인했습니다.

**Windows 보안 차단:** 최종 재검증 중 Windows가 원본 `site/brand/brand-x.js` 읽기를 “바이러스 또는 기타 사용자 동의 없이 설치된 소프트웨어” 사유로 차단했습니다. 차단 전 원본과의 12개 화면 비교 및 JS AST 검증은 통과했습니다. 마지막 화면 재비교에서는 원본 캔버스가 생성되지 않아 그 요소를 제외하고 비교했고, 수정본의 최종 JS는 이미 검증된 통합본과 비교했습니다. 보안 설정을 변경하거나 차단을 우회하지 않았으며, 해당 탐지가 오탐인지 여부는 판정하지 않았습니다.

## 전달 파일과 실행

압축파일에는 소스, GitHub Actions 설정, README, 이 변경 내역, 삭제 목록, 검증 기록이 들어 있습니다. 생성물 `dist/`와 작업 중 사용한 도구·테스트 이미지는 포함하지 않았습니다.

```sh
npm run build
npm start
```

`http://127.0.0.1:4175`에서 확인할 수 있습니다.
